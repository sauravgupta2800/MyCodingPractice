#include<bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> v={4,3,2,1,9,6,33,5};
    sort(v.begin(),v.end());
    for(int i=0;i<v.size()-1;i+=2)
    {
        swap(v[i],v[i+1]);
    }
    for(int i=0;i<v.size();i++)
    {
        cout<<v[i];
    }
}
